# Copy this file to ".env" and fill in your real values.
# NEVER commit the real .env file.

# Google Gemini API key (get one at https://makersuite.google.com/app/apikey)
GEMINI_API_KEY=your_gemini_api_key_here

# Gemini model endpoint
GEMINI_ENDPOINT=https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent

# Server port
PORT=3000

# Comma-separated list of origins allowed to call this API.
# Use the exact origin(s) your frontend is served from.
# Vite React client dev server runs on http://localhost:5173
# Example for local dev: http://localhost:5173,http://localhost:8000
#
# REQUIRED when NODE_ENV=production — the server will refuse to start if
# NODE_ENV is 'production' and this is left empty. In development (NODE_ENV
# unset or anything other than 'production'), it is ignored and any origin
# is allowed for convenience.
CORS_ORIGINS=http://localhost:5173,http://localhost:8000,http://127.0.0.1:8000

# ---- LLM reliability & cost controls ----
# All values below are integers. An invalid or out-of-range value is clamped
# to the safe bound (or default) with a startup warning rather than crashing,
# so a typo can't take the server down. Defaults are chosen to preserve normal
# answer quality while capping worst-case cost and latency.

# Timeout for a SINGLE Gemini API call, in milliseconds.
# Default 30000. Bounds: 1000–120000.
LLM_TIMEOUT_MS=30000

# Overall deadline for the ENTIRE /api/coach AI operation (initial call +
# retries + continuations combined), in milliseconds. Once exceeded, no
# further Gemini calls are made. Default 60000. Bounds: 5000–180000.
LLM_TOTAL_TIMEOUT_MS=60000

# Max retries for transient failures (429 / 5xx / network / timeout) PER
# logical call. Bounded further by LLM_MAX_CALLS_PER_REQUEST below.
# Default 2. Bounds: 0–5.
LLM_MAX_RETRIES=2

# Hard cap on the TOTAL number of Gemini API calls a single /api/coach request
# may make — shared across the initial call, every retry, and every
# continuation (and their retries). This is the ultimate cost ceiling per
# request. Default 8. Bounds: 1–20.
LLM_MAX_CALLS_PER_REQUEST=8

# Max number of continuation calls used to finish an answer that Gemini
# truncated at the output-token limit (long Indic-script answers may need a
# few). Also bounded by the call budget above. Default 4. Bounds: 0–8.
LLM_MAX_CONTINUATIONS=4

# Max output tokens Gemini may generate per call (supported generationConfig
# param). Kept high because Devanagari/Indic scripts use many tokens per
# character; continuation stitches longer answers together. Lowering this
# reduces cost but may truncate long answers sooner. Default 8192.
# Bounds: 256–8192.
LLM_MAX_OUTPUT_TOKENS=8192

# Rate limiting for POST /api/coach: window in minutes and max requests per
# window per IP. Leave RATE_LIMIT_MAX_REQUESTS unset to use the built-in
# environment-aware default: 60/window when NODE_ENV=production, 300/window
# otherwise (a single developer iterating on the UI can easily exceed 60 in
# 15 minutes; that shouldn't look like the same 429 a real abusive client
# would trigger). Set it explicitly to override either default — an explicit
# value always wins, in both dev and production.
RATE_LIMIT_WINDOW_MINUTES=15
RATE_LIMIT_MAX_REQUESTS=

# Rate limiting for /api/auth (all of it — login, register, refresh, /me, …),
# per IP, over a fixed 15-minute window that is deliberately independent of
# RATE_LIMIT_WINDOW_MINUTES above.
#
# Leave unset to use the environment-aware default: 30/window when
# NODE_ENV=production, 300/window otherwise. The production value is what slows
# down online credential guessing and should not be raised casually. The
# development value is higher because this limiter also counts POST /refresh
# and GET /me, which the client calls on EVERY page load — at 30, a developer
# reloading the app a dozen times exhausts the budget without a single failed
# sign-in and then sees "Too many attempts" on a perfectly correct password.
AUTH_RATE_LIMIT_MAX_REQUESTS=

# ---- Phase 2: database & auth ----

# SQLite database file (relative to the prisma/ folder). Swap for a PostgreSQL
# connection string later, e.g. postgresql://user:pass@host:5432/dbname
DATABASE_URL="file:./dev.db"

# Secret used to sign JWT access tokens. Use a long random string in production.
JWT_SECRET=change-me-to-a-long-random-secret

# How long an ACCESS token stays valid. Kept short — the client silently
# exchanges it for a new one via the refresh token below, so this mainly
# bounds how long a stolen access token stays usable if a session is revoked.
ACCESS_TOKEN_TTL=15m

# How long a REFRESH token stays valid before the user must log in again from
# scratch. This is the effective "how long can I stay logged in" setting —
# each successful refresh issues a new one, so an active user is never
# logged out mid-session. Refresh tokens are stored hashed in the Session
# table and can be revoked at any time (logout, "sign out other devices",
# or an admin's "revoke sessions" action).
REFRESH_TOKEN_TTL_DAYS=7

# Login security: lock an account after this many consecutive failed password
# attempts, for this many minutes.
LOGIN_MAX_ATTEMPTS=5
LOGIN_LOCKOUT_MINUTES=15

# ---- Transactional email (password reset) ----

# Brevo API key, used only to send password-reset emails.
# Get one at https://brevo.com (Settings -> SMTP & API -> API Keys). The free
# tier is enough for a pilot.
#
# Brevo was chosen because it verifies a single sender ADDRESS — a plain Gmail
# is enough. Providers that only verify a DOMAIN would mean buying one before
# any teacher could receive a reset link.
#
# Unlike GEMINI_API_KEY and JWT_SECRET, leaving this unset does NOT stop the
# server from starting: everything except password reset keeps working, and
# /auth/forgot-password still returns its usual generic response while logging
# that the send was skipped. Set it to actually deliver reset emails.
BREVO_API_KEY=

# The From address on reset emails, as `Name <address>`. The address must be
# verified in Brevo (Senders, Domains & Dedicated IPs -> Senders); an
# unverified sender makes every send fail with a 4xx.
EMAIL_FROM=Teacher Assistant <your-verified-sender@example.com>

# Public URL of the FRONTEND, used to build the link in a reset email
# (<APP_URL>/reset-password/<token>). Must match where the client is actually
# served, or the link in the email will 404.
APP_URL=http://localhost:5173

# How long a password-reset link stays valid, in minutes. Short by design —
# the link is a bearer credential sitting in an inbox.
PASSWORD_RESET_TTL_MINUTES=60

# ---- Google Sign-In ----

# OAuth 2.0 Web application Client ID from the Google Cloud Console
# (APIs & Services -> Credentials). The server verifies every Google ID token
# against this value as the expected audience, so a token minted for some other
# app can never be used to sign in here.
#
# The SAME value must be set as VITE_GOOGLE_CLIENT_ID in the client's env.
# Leaving this unset disables Google sign-in (POST /auth/google returns 503);
# email + password sign-in is unaffected.
GOOGLE_CLIENT_ID=

# ---- AI Action Router (assistant) ----
#
# The assistant lets a teacher type "Generate a Class 5 fractions worksheet" and
# lands them on the Generator with the fields pre-filled, ready to review and
# click Generate. It never generates, saves, or deletes anything by itself.
#
# EVERY gate below is OFF by default. A deployment that sets none of these ships
# a completely inert assistant — forgetting to configure it can only
# under-enable the feature, never over-enable it.
#
# Values are parsed leniently (see src/lib/flags.js): an unrecognized value falls
# back to the safe default with a startup warning rather than crashing, matching
# how the LLM tunables above behave. Booleans accept true/1/yes/on and
# false/0/no/off.

# MASTER KILL SWITCH. When false, both assistant endpoints are inert: the
# catalog returns empty and every interpret call falls straight through to the
# coach. This is the ONLY reliable incident control — the client is a PWA with
# service-worker caching, so a client-side flag change propagates on some later
# page load rather than immediately. Flipping this and restarting takes effect
# in under a minute.
# Default: false.
ASSISTANT_ENABLED=false

# Per-action gate. Each action descriptor names its own variable, so enabling
# the assistant does not implicitly enable every capability it will ever have.
# Default for each: false.
ASSISTANT_ACTION_GENERATE_ASSESSMENT=false
ASSISTANT_ACTION_OPEN_GENERATOR=false

# Comma-separated roles allowed to use the assistant at all. The catalog is
# filtered by this BEFORE the classifier prompt is built, so a teacher's prompt
# never contains actions they could not perform anyway.
# Default: teacher
ASSISTANT_ALLOWED_ROLES=teacher

# Comma-separated school codes for staged tenant rollout. EMPTY MEANS ALL
# SCHOOLS — this is a filter, not a gate (ASSISTANT_ENABLED is the gate).
# Default: empty.
ASSISTANT_ALLOWED_SCHOOL_CODES=

# Interpret calls one user may make per day. Bounds a single account's cost.
# Exhausting it degrades to normal coach answers, not an error.
#
# ENFORCED SINCE M9, and the counter lives in PROCESS MEMORY (no migration, no
# new datastore — Phase 1 ships zero migrations). Two consequences, stated
# because they matter operationally: the count RESETS ON RESTART, so a deploy
# hands everyone a fresh budget; and it is PER PROCESS, so running more than one
# instance makes the effective ceiling (limit x instances). This matches how the
# rate limiters in this app have always behaved (express-rate-limit's default
# MemoryStore) rather than introducing a new weakness — but revisit it BEFORE
# scaling to multiple instances, not after.
#
# Default 100. Bounds: 1-100000.
ASSISTANT_DAILY_BUDGET_PER_USER=100

# Per-IP request cap for /api/assistant/*, over the same window as
# RATE_LIMIT_WINDOW_MINUTES. A SEPARATE bucket from the /api/coach limiter on
# purpose: sharing one would let catalog fetches and routing decisions consume
# the budget a teacher needs for actual coaching answers. Higher than the coach
# ceiling because these calls are small and frequent rather than large and
# occasional.
# Default 120 in production, 600 in development. Bounds: 1-100000.
ASSISTANT_RATE_LIMIT_MAX_REQUESTS=120

# Per-IP request cap for POST /api/resources/generate (M9), over the same
# window as RATE_LIMIT_WINDOW_MINUTES. The generation endpoint is the most
# expensive path in the product and had no limiter at all before M9 — a
# pre-existing gap the AI router only makes cheaper to reach.
#
# 30 per 15 minutes is roughly two generations a minute sustained: far above a
# teacher preparing lessons, far below a loop. Keyed by IP like every other
# limiter here, so a school behind one NAT shares a bucket — an accepted Phase 1
# trade-off; per-user fairness is ASSISTANT_DAILY_BUDGET_PER_USER's job. Raise
# this first if a staff room reports 429s.
# Default 30 in production, 600 in development. Bounds: 1-100000.
RESOURCE_GENERATE_RATE_LIMIT_MAX=30

# ---- Assistant availability guard: router yields to Coach (M9, CHANGE-8) ----
#
# The Coach and the router draw on the SAME Gemini quota. When the upstream
# starts rate-limiting us, every routing call that fails is a call the Coach
# could have used to answer a teacher's question — so the router steps back and
# coaching keeps working (invariant I12).
#
# While the breaker is open, routing spends NO model calls and every message
# gets a normal coaching answer. /api/coach is never gated by this. The state is
# global to the process, not per user, and it re-closes on the first request
# after the cooldown elapses.
#
# How many rate-limited (HTTP 429) classifications open it.
# Default 5. Bounds: 1-100.
ASSISTANT_BREAKER_429_THRESHOLD=5
# How far back those are counted, in milliseconds.
# Default 60000 (1 minute). Bounds: 1000-3600000.
ASSISTANT_BREAKER_WINDOW_MS=60000
# How long it stays open once tripped, in milliseconds.
# Default 300000 (5 minutes). Bounds: 1000-3600000.
ASSISTANT_BREAKER_COOLDOWN_MS=300000

# ---- Assistant telemetry (M8) ----
#
# There is NO separate on/off switch for telemetry, and that is deliberate.
# POST /api/assistant/events sits behind the same rollout gate as the other two
# endpoints (ASSISTANT_ENABLED + role + school), so with the assistant off it
# writes nothing at all. A second flag would only create a state where routing
# runs unmeasured — which is the one configuration nobody should be able to
# reach by accident, because it is the configuration that produces no evidence.
#
# WHAT IS RECORDED: at most two `Event` rows per routed session — the prefill
# that was delivered, and what the teacher did with it. Counts, ids, enum values
# and field NAMES only. Never the utterance, a slot value, generated content,
# prompt text or model output (guardrail G11).
#
# RETENTION. Assistant telemetry is kept for 90 days and then pruned by
# `npm run assistant:prune-events`, which deletes ONLY the two assistant_* event
# types and can never reach safety flags or user-approval records. This value is
# the script's default; pass --days to override it for one run.
# Run it on a schedule (cron / task scheduler) once the feature is enabled —
# there is no in-process sweeper, on purpose: pruning inside a request would put
# deletes on the single-writer path this design works hard to keep quiet.
# Read the current numbers with `npm run assistant:metrics`.
ASSISTANT_EVENT_RETENTION_DAYS=90

# WRITE BOUND (M9). There is no separate flag for this either. Telemetry rows
# are capped per user per day at (ASSISTANT_DAILY_BUDGET_PER_USER x 2 + 20) —
# derived, because two rows per routed session is the design ceiling, so twice
# the routing budget cannot clip a legitimate teacher. The M9 security review
# added it after finding that this endpoint had no per-user bound at all: only
# the shared IP limiter stood in front of it, and each request may carry a batch
# of events. Over-budget batches are dropped silently (still 204) — telemetry is
# fire-and-forget by contract, so the bound can lose a measurement and can never
# cost a teacher anything. Kept separate from the routing budget so telemetry can
# never consume the allowance a teacher needs for actual routing.

# ---- Assistant LLM tunables (routing, NOT coaching) ----
#
# Routing runs on a SECOND GeminiService instance with its own, much tighter
# budget. The coaching values above (30s per call, 60s overall) are correct for
# generating a lesson plan and catastrophic for a routing decision that sits in
# front of a text box: a timeout here is a DECISION (fall through to the coach),
# not an error, so the budget must expire long before the teacher gives up.
#
# These take effect from milestone M5.

# Model endpoint used for routing only. A smaller/faster model is the right
# trade here — the task is short classification, not long-form writing.
#
# This is a FLOATING ALIAS ("-latest"), deliberately, and it is the one place in
# this project where a moving target was chosen over a pinned one. M0 documented
# `gemini-2.5-flash-lite`; by M5 that model returned 404 "no longer available to
# new users", which would have made every routing call fail on the day the
# feature was switched on. A pinned model is reproducible right up until it is
# retired without notice, and this endpoint sits behind a passthrough, so the
# cost of the alias moving under us is a quality wobble rather than an outage.
#
# CONSEQUENCE FOR M7, which must not be discovered later: an eval baseline
# recorded against an alias cannot be reproduced. The eval runner must capture
# the resolved model version reported by the API alongside its results, or the
# baseline says nothing about which model produced it.
#
# Note that models.list will happily list a model this key cannot actually call.
# Verify a model with a real generateContent request, never with the listing.
ASSISTANT_GEMINI_ENDPOINT=https://generativelanguage.googleapis.com/v1beta/models/gemini-flash-lite-latest:generateContent

# Timeout for a SINGLE routing call, in milliseconds.
# Default 3500. Bounds: 1000-10000.
ASSISTANT_LLM_TIMEOUT_MS=3500

# Overall deadline for the ENTIRE routing decision. Once exceeded, the request
# falls through to the coach.
# Default 5000. Bounds: 2000-15000.
ASSISTANT_LLM_TOTAL_TIMEOUT_MS=5000

# Retries for transient routing failures. Deliberately lower than coaching:
# retrying a routing decision the teacher is waiting on is rarely worth it.
# Default 1. Bounds: 0-2.
ASSISTANT_LLM_MAX_RETRIES=1

# Hard cap on Gemini calls per routing request (initial + retries).
# Default 2. Bounds: 1-3.
ASSISTANT_LLM_MAX_CALLS=2

# Max output tokens for a routing response. A classification result is a small
# JSON object; a large cap here would only pay for tokens that are never used.
# Default 512. Bounds: 128-1024.
ASSISTANT_LLM_MAX_OUTPUT_TOKENS=512

# ---- Multimodal attachments (Coach: image/PDF upload) ----------------------
#
# Lets a teacher attach a JPEG/PNG/WEBP image or a PDF to a Coach question
# ("Solve Question 5", "Summarize this chapter"). A sibling feature to the AI
# Action Router, not part of it — POST /api/coach/attachment is its own
# endpoint, sharing nothing with /api/assistant/*. Files are NEVER written to
# disk or the database: each upload is held in process memory only for the
# duration of the request, then discarded.
#
# Same kill-switch discipline as the assistant flags above: every gate below
# defaults OFF/closed, and ATTACHMENTS_ENABLED is the one reliable incident
# control (same PWA client-caching reasoning as ASSISTANT_ENABLED).

# MASTER KILL SWITCH. When false, POST /api/coach/attachment returns 503 for
# everyone and never buffers an upload. Existing text/voice Coach behavior is
# completely unaffected either way.
# Default: false.
ATTACHMENTS_ENABLED=false

# Comma-separated school codes for staged rollout, same shape as
# ASSISTANT_ALLOWED_SCHOOL_CODES. EMPTY MEANS ALL SCHOOLS — a filter, not a
# gate; ATTACHMENTS_ENABLED is the gate.
# Default: empty.
ATTACHMENT_ALLOWED_SCHOOL_CODES=

# Attachment requests one user may make per day. This is the most expensive
# call shape in the product (image/PDF tokens), so the default is deliberately
# lower than the router's own text-only classification budget. Lives in
# PROCESS MEMORY only (no migration) — resets on restart, and is PER PROCESS,
# matching every other in-memory counter in this app (see
# ASSISTANT_DAILY_BUDGET_PER_USER above for the full tradeoff explanation).
# Default 20. Bounds: 1-100000.
ATTACHMENT_DAILY_BUDGET_PER_USER=20

# Max upload size, in megabytes. Applies to both the multer body limit and
# the byte-level check in lib/fileValidation.js.
# Default 8. Bounds: 1-20.
ATTACHMENT_MAX_FILE_SIZE_MB=8

# Max ESTIMATED PDF page count (see lib/fileValidation.js — a lightweight
# marker count, not a real parse). A byte-size cap alone does not bound
# Gemini's per-page processing cost for a PDF, since a well-compressed
# multi-hundred-page PDF can be small in bytes. An unknown page count (an
# unusual PDF structure) is allowed through rather than rejected — this is a
# cost guard, not a correctness gate.
# Default 30. Bounds: 1-500.
ATTACHMENT_MAX_PDF_PAGES=30

# Max number of files a single message may attach — sent to Gemini TOGETHER in
# one call (see server/src/attachments/describeAttachment.js), not one call
# per file. Applies to both multer's own limit and the batch check in
# lib/fileValidation.js.
# Default 5. Bounds: 1-10.
ATTACHMENT_MAX_FILES=5

# Max COMBINED size of all files in one message, in megabytes. A SEPARATE
# guard from ATTACHMENT_MAX_FILE_SIZE_MB x ATTACHMENT_MAX_FILES — Gemini's
# inline-data request ceiling is a property of the whole request, and base64
# encoding adds ~33% on top of raw bytes, so several files each just under the
# per-file cap could still add up to a request too large or too slow to
# process well. 15MB raw stays comfortably under Gemini's ~20MB inline-request
# ceiling once that overhead is added.
# Default 15. Bounds: 1-40.
ATTACHMENT_MAX_TOTAL_SIZE_MB=15

# Per-IP request cap for POST /api/coach/attachment, over the same window as
# RATE_LIMIT_WINDOW_MINUTES. A SEPARATE bucket from /api/coach — attachment
# requests cost far more per call and must never be able to eat the budget a
# teacher needs for ordinary text/voice questions.
# Default 20 in production, 300 in development. Bounds: 1-100000.
ATTACHMENT_RATE_LIMIT_MAX_REQUESTS=20

# ---- Attachment LLM tunables (multimodal, NOT coaching or routing) --------
#
# A THIRD GeminiService instance, separate from both `gemini` (coaching) and
# `geminiFast` (routing) — see index.js. Multimodal calls are slower and more
# expensive than either, so they get their own timeout/retry/cost budget
# rather than sharing one sized for a different kind of request.

# Model endpoint for attachment requests. Defaults to the SAME endpoint as
# GEMINI_ENDPOINT (the coaching model) — Gemini 2.5 Flash already handles
# image/PDF input natively, so a separate model is not required unless you
# want to tune cost/quality independently later.
ATTACHMENT_GEMINI_ENDPOINT=

# Timeout for a SINGLE attachment call, in milliseconds.
# Default 30000. Bounds: 1000-120000.
ATTACHMENT_LLM_TIMEOUT_MS=30000

# Overall deadline for the ENTIRE attachment request (initial call + retries),
# in milliseconds.
# Default 60000. Bounds: 5000-180000.
ATTACHMENT_LLM_TOTAL_TIMEOUT_MS=60000

# Max retries for transient failures (429/5xx/network/timeout).
# Default 2. Bounds: 0-5.
ATTACHMENT_LLM_MAX_RETRIES=2

# Hard cap on total Gemini calls per attachment request.
# Default 8. Bounds: 1-20.
ATTACHMENT_LLM_MAX_CALLS_PER_REQUEST=8

# Max output tokens per attachment response.
# Default 4096. Bounds: 256-8192.
ATTACHMENT_LLM_MAX_OUTPUT_TOKENS=4096

# ---------------------------------------------------------------------------
# HELP & SUPPORT (bug reports + feedback) — see docs/help-support-architecture.md
# ---------------------------------------------------------------------------
#
# Lets a teacher report a bug or send feedback without leaving the app
# (POST /api/support/tickets). Phase 1 only: no file upload, no admin inbox,
# no AI-conversation opt-in — those are Phase 2. Makes no LLM call, so unlike
# the assistant/attachment flags above there is no daily-budget-per-user
# tunable; the shared per-IP SUPPORT_RATE_LIMIT_MAX_REQUESTS is what bounds it.
#
# Same kill-switch discipline as the flags above: defaults OFF/closed, and
# HELP_SUPPORT_ENABLED is the one reliable incident control.

# MASTER KILL SWITCH. When false, POST /api/support/tickets returns 503 for
# everyone. Existing feedback (thumbs up/down) is completely unaffected.
# Default: false.
HELP_SUPPORT_ENABLED=false

# Comma-separated school codes for staged rollout, same shape as
# ATTACHMENT_ALLOWED_SCHOOL_CODES. EMPTY MEANS ALL SCHOOLS — a filter, not a
# gate; HELP_SUPPORT_ENABLED is the gate.
# Default: empty.
HELP_SUPPORT_ALLOWED_SCHOOL_CODES=

# Requests to POST /api/support/tickets one IP may make per rate-limit window.
# Default: 20 in production, 300 in development. Bounds: 1-100000.
SUPPORT_RATE_LIMIT_MAX_REQUESTS=20

# ---------------------------------------------------------------------------
# AI LEARNING REPRESENTATION SYSTEM — see docs/learning-representation-system-adr.md
# ---------------------------------------------------------------------------
#
# Lets a teacher request a structured visual (diagram, table, timeline...)
# alongside an existing Coach answer (POST /api/coach/learning-representation).
# Same kill-switch discipline as the flags above: defaults OFF/closed, and
# LEARNING_REPRESENTATION_ENABLED is the one reliable incident control.

# MASTER KILL SWITCH. When false, the endpoint always returns its inert
# {representation: "verbal_explanation", data: null} response for everyone.
# Existing Coach answers are completely unaffected.
# Default: false.
LEARNING_REPRESENTATION_ENABLED=false

# Comma-separated school codes for staged rollout, same shape as
# ATTACHMENT_ALLOWED_SCHOOL_CODES. EMPTY MEANS ALL SCHOOLS — a filter, not a
# gate; LEARNING_REPRESENTATION_ENABLED is the gate.
# Default: empty.
LEARNING_REPRESENTATION_ALLOWED_SCHOOL_CODES=

# Learning-representation requests one user may make per day. Each request
# costs up to TWO Gemini calls (classify, then render), so this sits between
# ASSISTANT_DAILY_BUDGET_PER_USER (one call) and ATTACHMENT_DAILY_BUDGET_PER_USER
# (the most expensive call shape). Default: 50. Bounds: 1-100000.
LEARNING_REPRESENTATION_DAILY_BUDGET_PER_USER=50

# Requests to POST /api/coach/learning-representation one IP may make per
# rate-limit window. Default: 60 in production, 600 in development. Bounds: 1-100000.
LEARNING_REPRESENTATION_RATE_LIMIT_MAX_REQUESTS=60
# CLASSROOM MODE — see docs/classroom-mode.md
# ---------------------------------------------------------------------------
#
# A chat mode a teacher turns on from the "+" button in the Composer. While it
# is on, a question that contains a teachable topic also produces classroom
# materials (lesson plan, worksheet, quiz, homework, exit ticket) alongside the
# normal coaching answer.
#
# This is the ONE feature where a single teacher action fans out into several
# model calls — a coaching answer, a small planner call, then one generation per
# applicable artifact. CLASSROOM_MODE_ENABLED is therefore a spend control as
# well as a kill switch, and it is the only reliable one: the client's
# VITE_CLASSROOM_MODE_ENABLED is baked in at build time and cannot be trusted to
# have reached already-loaded PWA clients.

# MASTER KILL SWITCH. When false, /api/coach never runs the planner and never
# attaches classroom materials, whatever the client sends. Ordinary coaching
# answers are completely unaffected.
# Default: false.
CLASSROOM_MODE_ENABLED=false

# Comma-separated school codes for staged rollout, same shape as
# ATTACHMENT_ALLOWED_SCHOOL_CODES. EMPTY MEANS ALL SCHOOLS — a filter, not a
# gate; CLASSROOM_MODE_ENABLED is the gate.
# Default: empty.
CLASSROOM_MODE_ALLOWED_SCHOOL_CODES=

# ---------------------------------------------------------------------------
# PYQ QUESTION PAPER INTELLIGENCE — see docs/pyq-implementation-plan.md
# ---------------------------------------------------------------------------
#
# Lets an admin build a question paper from real previous-year exam questions
# instead of (or blended with) AI-generated ones. Ingestion (PDF upload →
# Gemini extraction → human review → publish) is admin-only and role-gated
# (requireRole('super_admin')) — it is NEVER gated by PYQ_ENABLED below. Only
# the TEACHER-FACING read/generation endpoints, added in a later phase, are
# gated by this flag — same "write access is role-gated; read access is
# feature-flag-gated" split documented in the plan's §12.
#
# Phase 2 (current): admin upload/list/get/source only. PYQ_ENABLED and
# PYQ_ALLOWED_SCHOOL_CODES below are defined now but not yet consumed by any
# route — same "constructed now, used by a later phase" precedent as the
# PYQ_EXTRACTION_LLM_* tunables further down.

# Gates the teacher-facing PYQ read/generation endpoints added in a later
# phase. Has NO effect on admin ingestion (routes/adminPyq.js), which is
# always reachable by a super_admin regardless of this flag.
# Default: false.
PYQ_ENABLED=false

# Comma-separated school codes for staged rollout of the teacher-facing PYQ
# endpoints, same shape as ATTACHMENT_ALLOWED_SCHOOL_CODES. EMPTY MEANS ALL
# SCHOOLS — a filter, not a gate; PYQ_ENABLED is the gate.
# Default: empty.
PYQ_ALLOWED_SCHOOL_CODES=

# Max source-PDF upload size, in megabytes. A full scanned board-exam paper is
# a bigger document than a single chat attachment (ATTACHMENT_MAX_FILE_SIZE_MB
# above), so this ceiling is deliberately separate and higher.
# Default: 25. Bounds: 1-100.
PYQ_MAX_FILE_SIZE_MB=25

# Max ESTIMATED PDF page count for a source document (same lightweight marker
# count as ATTACHMENT_MAX_PDF_PAGES, not a real parse — see
# lib/fileValidation.js). Generous headroom over a realistic ~10-page exam
# paper, while still rejecting an implausibly huge (almost certainly
# mis-uploaded) file.
# Default: 60. Bounds: 1-500.
PYQ_MAX_PDF_PAGES=60

# ---- PYQ extraction LLM tunables (NOT used until Phase 3) -----------------
#
# A FOURTH GeminiService instance, separate from `gemini` (coaching),
# `geminiFast` (routing), and `attachmentGemini` (multimodal Coach
# attachments) — see index.js. Per-page PDF extraction produces a materially
# different call shape (a whole page's structured JSON output) from any of
# those three, so it gets its own budget rather than sharing one sized for a
# different kind of request. Constructed in Phase 2 but genuinely unused
# until Phase 3 (Extraction) exists to call it.

# Model endpoint for PYQ extraction calls. Defaults to the SAME endpoint as
# GEMINI_ENDPOINT — Gemini 2.5 Flash already handles PDF input natively.
PYQ_GEMINI_ENDPOINT=

# Timeout for a SINGLE extraction call (one page), in milliseconds.
# Default 30000. Bounds: 1000-120000.
PYQ_EXTRACTION_LLM_TIMEOUT_MS=30000

# Overall deadline for one page's extraction (initial call + retries), in
# milliseconds.
# Default 60000. Bounds: 5000-180000.
PYQ_EXTRACTION_LLM_TOTAL_TIMEOUT_MS=60000

# Max retries for transient failures (429/5xx/network/timeout) per page.
# Default 2. Bounds: 0-5.
PYQ_EXTRACTION_LLM_MAX_RETRIES=2

# Hard cap on total Gemini calls per page-extraction request.
# Default 8. Bounds: 1-20.
PYQ_EXTRACTION_LLM_MAX_CALLS_PER_REQUEST=8

# Max output tokens per extraction response.
# Default 8192. Bounds: 256-8192.
PYQ_EXTRACTION_LLM_MAX_OUTPUT_TOKENS=8192

# ---- PYQ clustering / embeddings (Phase 6, offline batch scripts only) ----
#
# Gemini's EMBEDDING endpoint — a different API shape from every
# GeminiService instance above (embedContent, not generateContent), so it is
# NOT one of the four GeminiService instances in index.js. Called only from
# pyqEmbedBatch.js (`npm run embed:pyq`), an operator-invoked offline script,
# never from a live HTTP route — see lib/pyqClustering.js's embedText.
# Defaults to Gemini's text-embedding-004 model.
PYQ_EMBEDDING_ENDPOINT=

# ---- PYQ generation (Phase 8) ----
#
# Rate limit for POST /api/resources/generate-pyq — its OWN bucket, not
# shared with RESOURCE_GENERATE_RATE_LIMIT_MAX (the AI /resources/generate
# path): this endpoint makes NO Gemini call at all (selectPyqPaper is pure/
# deterministic, §10/§11), so it is cheaper per-request and gets a higher
# default ceiling. See index.js's pyqGenerateLimiter for the full reasoning.
PYQ_GENERATE_RATE_LIMIT_MAX_REQUESTS=
